namespace PrisonCorruptDepthstest.Entities;

public static class EntitiesConstants
{
    // No custom mobs in this mod — uses vanilla mobs via CDB mob table.
    // This file exists for future custom entity additions.
}
